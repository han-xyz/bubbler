[package]
name = "bubbler-wl-proxy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Wayland wire proxy for bubbler sandboxes: parses the protocol so the clipboard can be gated"

[lib]
path = "src/lib.rs"

[[bin]]
name = "bubbler-wl-proxy"
path = "src/main.rs"

[dependencies]
thiserror.workspace = true

[build-dependencies]
wayrs-proto-parser.workspace = true
