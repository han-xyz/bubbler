[package]
name = "bubbler-init"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Minimal in-sandbox supervisor for bubbler: runs the command, serves exec requests"

[lib]
path = "src/lib.rs"

[[bin]]
name = "bubbler-init"
path = "src/main.rs"

[dependencies]
rustix.workspace = true
signal-hook.workspace = true

[dev-dependencies]
tempfile.workspace = true
